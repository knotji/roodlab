import { NextResponse } from "next/server";import { readCatalog,writeCatalogAudit } from "@/lib/cache";import { auditCatalog } from "@/lib/data-sources/allhuay/audit";
export async function POST(){try{const report=await auditCatalog(await readCatalog());await writeCatalogAudit(report);return NextResponse.json({ok:true,...report})}catch(error){return NextResponse.json({ok:false,error:error instanceof Error?error.message:"audit failed"},{status:500})}}
