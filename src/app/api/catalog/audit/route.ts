import { NextResponse } from "next/server";import { promises as fs } from "node:fs";import path from "node:path";import { readCatalog } from "@/lib/cache";import { auditCatalog } from "@/lib/data-sources/allhuay/audit";
export async function POST(){try{const report=await auditCatalog(await readCatalog());await fs.writeFile(path.join(process.cwd(),"data","catalog-audit.json"),JSON.stringify(report,null,2),"utf8");return NextResponse.json({ok:true,...report})}catch(error){return NextResponse.json({ok:false,error:error instanceof Error?error.message:"audit failed"},{status:500})}}
