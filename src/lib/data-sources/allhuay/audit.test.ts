import { afterEach,describe,expect,it,vi } from "vitest";import { auditCatalog } from "./audit";import type { LotteryDefinition } from "../../types";
const catalog:LotteryDefinition[]=[{id:"ok",name:"OK",slug:"ok",category:"test",sourceUrl:"https://example.com/ok"},{id:"bad",name:"Bad",slug:"bad",category:"test",sourceUrl:"https://example.com/bad"}];afterEach(()=>vi.unstubAllGlobals());
describe("catalog audit",()=>{it("classifies supported and failed sources",async()=>{vi.stubGlobal("fetch",vi.fn(async(url:string)=>url.endsWith("/bad")?new Response("",{status:500}):new Response(`<table><tr><td>2 สิงหาคม 2569</td><td>12345</td><td>345</td><td>45</td><td>07</td></tr></table>`,{status:200})));const report=await auditCatalog(catalog,2);expect(report).toMatchObject({total:2,supported:1,failed:1,partial:0});expect(report.items.find(x=>x.id==="bad")?.reason).toBe("HTTP 500")})});
